// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: webhooks.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createWebhook = `-- name: CreateWebhook :one

INSERT INTO webhooks (
    shop_id, topic, target_url, secret_key, is_active
) VALUES (
    $1, $2, $3, $4, $5
)
RETURNING id, shop_id, topic, target_url, secret_key, is_active
`

type CreateWebhookParams struct {
	ShopID    pgtype.UUID `json:"shop_id"`
	Topic     string      `json:"topic"`
	TargetUrl string      `json:"target_url"`
	SecretKey string      `json:"secret_key"`
	IsActive  pgtype.Bool `json:"is_active"`
}

// ==================== Webhooks ====================
func (q *Queries) CreateWebhook(ctx context.Context, arg CreateWebhookParams) (Webhook, error) {
	row := q.db.QueryRow(ctx, createWebhook,
		arg.ShopID,
		arg.Topic,
		arg.TargetUrl,
		arg.SecretKey,
		arg.IsActive,
	)
	var i Webhook
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.Topic,
		&i.TargetUrl,
		&i.SecretKey,
		&i.IsActive,
	)
	return i, err
}

const deleteWebhook = `-- name: DeleteWebhook :exec
DELETE FROM webhooks WHERE id = $1 AND shop_id = $2
`

type DeleteWebhookParams struct {
	ID     uuid.UUID   `json:"id"`
	ShopID pgtype.UUID `json:"shop_id"`
}

func (q *Queries) DeleteWebhook(ctx context.Context, arg DeleteWebhookParams) error {
	_, err := q.db.Exec(ctx, deleteWebhook, arg.ID, arg.ShopID)
	return err
}

const getWebhook = `-- name: GetWebhook :one
SELECT id, shop_id, topic, target_url, secret_key, is_active FROM webhooks
WHERE id = $1 AND shop_id = $2
`

type GetWebhookParams struct {
	ID     uuid.UUID   `json:"id"`
	ShopID pgtype.UUID `json:"shop_id"`
}

func (q *Queries) GetWebhook(ctx context.Context, arg GetWebhookParams) (Webhook, error) {
	row := q.db.QueryRow(ctx, getWebhook, arg.ID, arg.ShopID)
	var i Webhook
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.Topic,
		&i.TargetUrl,
		&i.SecretKey,
		&i.IsActive,
	)
	return i, err
}

const listActiveWebhooks = `-- name: ListActiveWebhooks :many
SELECT id, shop_id, topic, target_url, secret_key, is_active FROM webhooks
WHERE shop_id = $1 AND is_active = TRUE
ORDER BY topic ASC
`

func (q *Queries) ListActiveWebhooks(ctx context.Context, shopID pgtype.UUID) ([]Webhook, error) {
	rows, err := q.db.Query(ctx, listActiveWebhooks, shopID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Webhook
	for rows.Next() {
		var i Webhook
		if err := rows.Scan(
			&i.ID,
			&i.ShopID,
			&i.Topic,
			&i.TargetUrl,
			&i.SecretKey,
			&i.IsActive,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listWebhooks = `-- name: ListWebhooks :many
SELECT id, shop_id, topic, target_url, secret_key, is_active FROM webhooks
WHERE shop_id = $1
ORDER BY topic ASC
`

func (q *Queries) ListWebhooks(ctx context.Context, shopID pgtype.UUID) ([]Webhook, error) {
	rows, err := q.db.Query(ctx, listWebhooks, shopID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Webhook
	for rows.Next() {
		var i Webhook
		if err := rows.Scan(
			&i.ID,
			&i.ShopID,
			&i.Topic,
			&i.TargetUrl,
			&i.SecretKey,
			&i.IsActive,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listWebhooksByTopic = `-- name: ListWebhooksByTopic :many
SELECT id, shop_id, topic, target_url, secret_key, is_active FROM webhooks
WHERE shop_id = $1 AND topic = $2 AND is_active = TRUE
`

type ListWebhooksByTopicParams struct {
	ShopID pgtype.UUID `json:"shop_id"`
	Topic  string      `json:"topic"`
}

func (q *Queries) ListWebhooksByTopic(ctx context.Context, arg ListWebhooksByTopicParams) ([]Webhook, error) {
	rows, err := q.db.Query(ctx, listWebhooksByTopic, arg.ShopID, arg.Topic)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Webhook
	for rows.Next() {
		var i Webhook
		if err := rows.Scan(
			&i.ID,
			&i.ShopID,
			&i.Topic,
			&i.TargetUrl,
			&i.SecretKey,
			&i.IsActive,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const toggleWebhook = `-- name: ToggleWebhook :one
UPDATE webhooks SET is_active = NOT is_active
WHERE id = $1 AND shop_id = $2
RETURNING id, shop_id, topic, target_url, secret_key, is_active
`

type ToggleWebhookParams struct {
	ID     uuid.UUID   `json:"id"`
	ShopID pgtype.UUID `json:"shop_id"`
}

func (q *Queries) ToggleWebhook(ctx context.Context, arg ToggleWebhookParams) (Webhook, error) {
	row := q.db.QueryRow(ctx, toggleWebhook, arg.ID, arg.ShopID)
	var i Webhook
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.Topic,
		&i.TargetUrl,
		&i.SecretKey,
		&i.IsActive,
	)
	return i, err
}

const updateWebhook = `-- name: UpdateWebhook :one
UPDATE webhooks SET
    topic = COALESCE($1, topic),
    target_url = COALESCE($2, target_url),
    secret_key = COALESCE($3, secret_key),
    is_active = COALESCE($4, is_active)
WHERE id = $5 AND shop_id = $6
RETURNING id, shop_id, topic, target_url, secret_key, is_active
`

type UpdateWebhookParams struct {
	Topic     pgtype.Text `json:"topic"`
	TargetUrl pgtype.Text `json:"target_url"`
	SecretKey pgtype.Text `json:"secret_key"`
	IsActive  pgtype.Bool `json:"is_active"`
	ID        uuid.UUID   `json:"id"`
	ShopID    pgtype.UUID `json:"shop_id"`
}

func (q *Queries) UpdateWebhook(ctx context.Context, arg UpdateWebhookParams) (Webhook, error) {
	row := q.db.QueryRow(ctx, updateWebhook,
		arg.Topic,
		arg.TargetUrl,
		arg.SecretKey,
		arg.IsActive,
		arg.ID,
		arg.ShopID,
	)
	var i Webhook
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.Topic,
		&i.TargetUrl,
		&i.SecretKey,
		&i.IsActive,
	)
	return i, err
}
