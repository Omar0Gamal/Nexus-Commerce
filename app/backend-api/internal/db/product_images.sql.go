// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: product_images.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createImage = `-- name: CreateImage :one

INSERT INTO product_images (
    product_id, shop_id, variant_id, url, alt_text, position, is_primary
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
RETURNING id, product_id, shop_id, variant_id, url, alt_text, position, is_primary, created_at
`

type CreateImageParams struct {
	ProductID uuid.UUID   `json:"product_id"`
	ShopID    uuid.UUID   `json:"shop_id"`
	VariantID pgtype.UUID `json:"variant_id"`
	Url       string      `json:"url"`
	AltText   pgtype.Text `json:"alt_text"`
	Position  pgtype.Int4 `json:"position"`
	IsPrimary pgtype.Bool `json:"is_primary"`
}

// ==================== Product Images ====================
func (q *Queries) CreateImage(ctx context.Context, arg CreateImageParams) (ProductImage, error) {
	row := q.db.QueryRow(ctx, createImage,
		arg.ProductID,
		arg.ShopID,
		arg.VariantID,
		arg.Url,
		arg.AltText,
		arg.Position,
		arg.IsPrimary,
	)
	var i ProductImage
	err := row.Scan(
		&i.ID,
		&i.ProductID,
		&i.ShopID,
		&i.VariantID,
		&i.Url,
		&i.AltText,
		&i.Position,
		&i.IsPrimary,
		&i.CreatedAt,
	)
	return i, err
}

const deleteImage = `-- name: DeleteImage :exec
DELETE FROM product_images WHERE id = $1 AND shop_id = $2
`

type DeleteImageParams struct {
	ID     uuid.UUID `json:"id"`
	ShopID uuid.UUID `json:"shop_id"`
}

func (q *Queries) DeleteImage(ctx context.Context, arg DeleteImageParams) error {
	_, err := q.db.Exec(ctx, deleteImage, arg.ID, arg.ShopID)
	return err
}

const deleteImagesByProduct = `-- name: DeleteImagesByProduct :exec
DELETE FROM product_images WHERE product_id = $1 AND shop_id = $2
`

type DeleteImagesByProductParams struct {
	ProductID uuid.UUID `json:"product_id"`
	ShopID    uuid.UUID `json:"shop_id"`
}

func (q *Queries) DeleteImagesByProduct(ctx context.Context, arg DeleteImagesByProductParams) error {
	_, err := q.db.Exec(ctx, deleteImagesByProduct, arg.ProductID, arg.ShopID)
	return err
}

const getImage = `-- name: GetImage :one
SELECT id, product_id, shop_id, variant_id, url, alt_text, position, is_primary, created_at FROM product_images
WHERE id = $1 AND shop_id = $2
`

type GetImageParams struct {
	ID     uuid.UUID `json:"id"`
	ShopID uuid.UUID `json:"shop_id"`
}

func (q *Queries) GetImage(ctx context.Context, arg GetImageParams) (ProductImage, error) {
	row := q.db.QueryRow(ctx, getImage, arg.ID, arg.ShopID)
	var i ProductImage
	err := row.Scan(
		&i.ID,
		&i.ProductID,
		&i.ShopID,
		&i.VariantID,
		&i.Url,
		&i.AltText,
		&i.Position,
		&i.IsPrimary,
		&i.CreatedAt,
	)
	return i, err
}

const getImagesByProductIDs = `-- name: GetImagesByProductIDs :many
SELECT id, product_id, shop_id, variant_id, url, alt_text, position, is_primary, created_at FROM product_images
WHERE product_id = ANY($1::uuid[]) AND shop_id = $2
ORDER BY product_id, position ASC, created_at ASC
`

type GetImagesByProductIDsParams struct {
	Column1 []uuid.UUID `json:"column_1"`
	ShopID  uuid.UUID   `json:"shop_id"`
}

func (q *Queries) GetImagesByProductIDs(ctx context.Context, arg GetImagesByProductIDsParams) ([]ProductImage, error) {
	rows, err := q.db.Query(ctx, getImagesByProductIDs, arg.Column1, arg.ShopID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ProductImage
	for rows.Next() {
		var i ProductImage
		if err := rows.Scan(
			&i.ID,
			&i.ProductID,
			&i.ShopID,
			&i.VariantID,
			&i.Url,
			&i.AltText,
			&i.Position,
			&i.IsPrimary,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listImagesByProduct = `-- name: ListImagesByProduct :many
SELECT id, product_id, shop_id, variant_id, url, alt_text, position, is_primary, created_at FROM product_images
WHERE product_id = $1 AND shop_id = $2
ORDER BY position ASC, created_at ASC
`

type ListImagesByProductParams struct {
	ProductID uuid.UUID `json:"product_id"`
	ShopID    uuid.UUID `json:"shop_id"`
}

func (q *Queries) ListImagesByProduct(ctx context.Context, arg ListImagesByProductParams) ([]ProductImage, error) {
	rows, err := q.db.Query(ctx, listImagesByProduct, arg.ProductID, arg.ShopID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ProductImage
	for rows.Next() {
		var i ProductImage
		if err := rows.Scan(
			&i.ID,
			&i.ProductID,
			&i.ShopID,
			&i.VariantID,
			&i.Url,
			&i.AltText,
			&i.Position,
			&i.IsPrimary,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listImagesByVariant = `-- name: ListImagesByVariant :many
SELECT id, product_id, shop_id, variant_id, url, alt_text, position, is_primary, created_at FROM product_images
WHERE variant_id = $1 AND shop_id = $2
ORDER BY position ASC
`

type ListImagesByVariantParams struct {
	VariantID pgtype.UUID `json:"variant_id"`
	ShopID    uuid.UUID   `json:"shop_id"`
}

func (q *Queries) ListImagesByVariant(ctx context.Context, arg ListImagesByVariantParams) ([]ProductImage, error) {
	rows, err := q.db.Query(ctx, listImagesByVariant, arg.VariantID, arg.ShopID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ProductImage
	for rows.Next() {
		var i ProductImage
		if err := rows.Scan(
			&i.ID,
			&i.ProductID,
			&i.ShopID,
			&i.VariantID,
			&i.Url,
			&i.AltText,
			&i.Position,
			&i.IsPrimary,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setPrimaryImage = `-- name: SetPrimaryImage :exec
UPDATE product_images SET is_primary = (id = $1)
WHERE product_id = $2 AND shop_id = $3
`

type SetPrimaryImageParams struct {
	ID        uuid.UUID `json:"id"`
	ProductID uuid.UUID `json:"product_id"`
	ShopID    uuid.UUID `json:"shop_id"`
}

func (q *Queries) SetPrimaryImage(ctx context.Context, arg SetPrimaryImageParams) error {
	_, err := q.db.Exec(ctx, setPrimaryImage, arg.ID, arg.ProductID, arg.ShopID)
	return err
}

const updateImage = `-- name: UpdateImage :one
UPDATE product_images SET
    alt_text = COALESCE($1, alt_text),
    position = COALESCE($2, position),
    is_primary = COALESCE($3, is_primary),
    variant_id = COALESCE($4, variant_id)
WHERE id = $5 AND shop_id = $6
RETURNING id, product_id, shop_id, variant_id, url, alt_text, position, is_primary, created_at
`

type UpdateImageParams struct {
	AltText   pgtype.Text `json:"alt_text"`
	Position  pgtype.Int4 `json:"position"`
	IsPrimary pgtype.Bool `json:"is_primary"`
	VariantID pgtype.UUID `json:"variant_id"`
	ID        uuid.UUID   `json:"id"`
	ShopID    uuid.UUID   `json:"shop_id"`
}

func (q *Queries) UpdateImage(ctx context.Context, arg UpdateImageParams) (ProductImage, error) {
	row := q.db.QueryRow(ctx, updateImage,
		arg.AltText,
		arg.Position,
		arg.IsPrimary,
		arg.VariantID,
		arg.ID,
		arg.ShopID,
	)
	var i ProductImage
	err := row.Scan(
		&i.ID,
		&i.ProductID,
		&i.ShopID,
		&i.VariantID,
		&i.Url,
		&i.AltText,
		&i.Position,
		&i.IsPrimary,
		&i.CreatedAt,
	)
	return i, err
}
