// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: plans.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createPlan = `-- name: CreatePlan :one

INSERT INTO plans (
    name, monthly_price, max_products, max_staff_accounts,
    max_storage_mb, transaction_fee_percent, features
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
RETURNING id, name, monthly_price, max_products, max_staff_accounts, max_storage_mb, transaction_fee_percent, features, created_at
`

type CreatePlanParams struct {
	Name                  string         `json:"name"`
	MonthlyPrice          pgtype.Numeric `json:"monthly_price"`
	MaxProducts           pgtype.Int4    `json:"max_products"`
	MaxStaffAccounts      pgtype.Int4    `json:"max_staff_accounts"`
	MaxStorageMb          pgtype.Int4    `json:"max_storage_mb"`
	TransactionFeePercent pgtype.Numeric `json:"transaction_fee_percent"`
	Features              []byte         `json:"features"`
}

// ==================== Plans ====================
func (q *Queries) CreatePlan(ctx context.Context, arg CreatePlanParams) (Plan, error) {
	row := q.db.QueryRow(ctx, createPlan,
		arg.Name,
		arg.MonthlyPrice,
		arg.MaxProducts,
		arg.MaxStaffAccounts,
		arg.MaxStorageMb,
		arg.TransactionFeePercent,
		arg.Features,
	)
	var i Plan
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.MonthlyPrice,
		&i.MaxProducts,
		&i.MaxStaffAccounts,
		&i.MaxStorageMb,
		&i.TransactionFeePercent,
		&i.Features,
		&i.CreatedAt,
	)
	return i, err
}

const deletePlan = `-- name: DeletePlan :exec
DELETE FROM plans WHERE id = $1
`

func (q *Queries) DeletePlan(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deletePlan, id)
	return err
}

const getPlan = `-- name: GetPlan :one
SELECT id, name, monthly_price, max_products, max_staff_accounts, max_storage_mb, transaction_fee_percent, features, created_at FROM plans WHERE id = $1
`

func (q *Queries) GetPlan(ctx context.Context, id uuid.UUID) (Plan, error) {
	row := q.db.QueryRow(ctx, getPlan, id)
	var i Plan
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.MonthlyPrice,
		&i.MaxProducts,
		&i.MaxStaffAccounts,
		&i.MaxStorageMb,
		&i.TransactionFeePercent,
		&i.Features,
		&i.CreatedAt,
	)
	return i, err
}

const getPlanByName = `-- name: GetPlanByName :one
SELECT id, name, monthly_price, max_products, max_staff_accounts, max_storage_mb, transaction_fee_percent, features, created_at FROM plans WHERE name = $1
`

func (q *Queries) GetPlanByName(ctx context.Context, name string) (Plan, error) {
	row := q.db.QueryRow(ctx, getPlanByName, name)
	var i Plan
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.MonthlyPrice,
		&i.MaxProducts,
		&i.MaxStaffAccounts,
		&i.MaxStorageMb,
		&i.TransactionFeePercent,
		&i.Features,
		&i.CreatedAt,
	)
	return i, err
}

const listPlans = `-- name: ListPlans :many
SELECT id, name, monthly_price, max_products, max_staff_accounts, max_storage_mb, transaction_fee_percent, features, created_at FROM plans ORDER BY monthly_price ASC
`

func (q *Queries) ListPlans(ctx context.Context) ([]Plan, error) {
	rows, err := q.db.Query(ctx, listPlans)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Plan
	for rows.Next() {
		var i Plan
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.MonthlyPrice,
			&i.MaxProducts,
			&i.MaxStaffAccounts,
			&i.MaxStorageMb,
			&i.TransactionFeePercent,
			&i.Features,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updatePlan = `-- name: UpdatePlan :one
UPDATE plans SET
    name = COALESCE($1, name),
    monthly_price = COALESCE($2, monthly_price),
    max_products = COALESCE($3, max_products),
    max_staff_accounts = COALESCE($4, max_staff_accounts),
    max_storage_mb = COALESCE($5, max_storage_mb),
    transaction_fee_percent = COALESCE($6, transaction_fee_percent),
    features = COALESCE($7, features)
WHERE id = $8
RETURNING id, name, monthly_price, max_products, max_staff_accounts, max_storage_mb, transaction_fee_percent, features, created_at
`

type UpdatePlanParams struct {
	Name                  pgtype.Text    `json:"name"`
	MonthlyPrice          pgtype.Numeric `json:"monthly_price"`
	MaxProducts           pgtype.Int4    `json:"max_products"`
	MaxStaffAccounts      pgtype.Int4    `json:"max_staff_accounts"`
	MaxStorageMb          pgtype.Int4    `json:"max_storage_mb"`
	TransactionFeePercent pgtype.Numeric `json:"transaction_fee_percent"`
	Features              []byte         `json:"features"`
	ID                    uuid.UUID      `json:"id"`
}

func (q *Queries) UpdatePlan(ctx context.Context, arg UpdatePlanParams) (Plan, error) {
	row := q.db.QueryRow(ctx, updatePlan,
		arg.Name,
		arg.MonthlyPrice,
		arg.MaxProducts,
		arg.MaxStaffAccounts,
		arg.MaxStorageMb,
		arg.TransactionFeePercent,
		arg.Features,
		arg.ID,
	)
	var i Plan
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.MonthlyPrice,
		&i.MaxProducts,
		&i.MaxStaffAccounts,
		&i.MaxStorageMb,
		&i.TransactionFeePercent,
		&i.Features,
		&i.CreatedAt,
	)
	return i, err
}
