// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: staff_audit_log.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createStaffAuditLog = `-- name: CreateStaffAuditLog :one

INSERT INTO staff_audit_log (
    shop_id, staff_id, action, resource_type, resource_id, metadata, ip_address
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
)
RETURNING id, shop_id, staff_id, action, resource_type, resource_id, metadata, ip_address, created_at
`

type CreateStaffAuditLogParams struct {
	ShopID       uuid.UUID   `json:"shop_id"`
	StaffID      pgtype.UUID `json:"staff_id"`
	Action       string      `json:"action"`
	ResourceType string      `json:"resource_type"`
	ResourceID   pgtype.UUID `json:"resource_id"`
	Metadata     []byte      `json:"metadata"`
	IpAddress    pgtype.Text `json:"ip_address"`
}

// ==================== Staff Audit Log ====================
func (q *Queries) CreateStaffAuditLog(ctx context.Context, arg CreateStaffAuditLogParams) (StaffAuditLog, error) {
	row := q.db.QueryRow(ctx, createStaffAuditLog,
		arg.ShopID,
		arg.StaffID,
		arg.Action,
		arg.ResourceType,
		arg.ResourceID,
		arg.Metadata,
		arg.IpAddress,
	)
	var i StaffAuditLog
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.StaffID,
		&i.Action,
		&i.ResourceType,
		&i.ResourceID,
		&i.Metadata,
		&i.IpAddress,
		&i.CreatedAt,
	)
	return i, err
}

const listStaffAuditLog = `-- name: ListStaffAuditLog :many
SELECT id, shop_id, staff_id, action, resource_type, resource_id, metadata, ip_address, created_at FROM staff_audit_log
WHERE shop_id = $1
  AND ($2::uuid IS NULL OR staff_id = $2)
  AND ($3::text  = '' OR resource_type = $3)
ORDER BY created_at DESC
LIMIT $4 OFFSET $5
`

type ListStaffAuditLogParams struct {
	ShopID  uuid.UUID `json:"shop_id"`
	Column2 uuid.UUID `json:"column_2"`
	Column3 string    `json:"column_3"`
	Limit   int32     `json:"limit"`
	Offset  int32     `json:"offset"`
}

func (q *Queries) ListStaffAuditLog(ctx context.Context, arg ListStaffAuditLogParams) ([]StaffAuditLog, error) {
	rows, err := q.db.Query(ctx, listStaffAuditLog,
		arg.ShopID,
		arg.Column2,
		arg.Column3,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []StaffAuditLog
	for rows.Next() {
		var i StaffAuditLog
		if err := rows.Scan(
			&i.ID,
			&i.ShopID,
			&i.StaffID,
			&i.Action,
			&i.ResourceType,
			&i.ResourceID,
			&i.Metadata,
			&i.IpAddress,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
