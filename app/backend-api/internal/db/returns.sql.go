// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: returns.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const countReturns = `-- name: CountReturns :one
SELECT COUNT(*) FROM order_returns
WHERE shop_id = $1
`

func (q *Queries) CountReturns(ctx context.Context, shopID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countReturns, shopID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createReturn = `-- name: CreateReturn :one

INSERT INTO order_returns (shop_id, order_id, customer_id, reason, status)
VALUES ($1, $2, $3, $4, 'requested')
RETURNING id, shop_id, order_id, customer_id, reason, status, paymob_refund_id, refund_amount, notes, created_at, updated_at
`

type CreateReturnParams struct {
	ShopID     uuid.UUID   `json:"shop_id"`
	OrderID    uuid.UUID   `json:"order_id"`
	CustomerID pgtype.UUID `json:"customer_id"`
	Reason     string      `json:"reason"`
}

// ==================== Order Returns ====================
func (q *Queries) CreateReturn(ctx context.Context, arg CreateReturnParams) (OrderReturn, error) {
	row := q.db.QueryRow(ctx, createReturn,
		arg.ShopID,
		arg.OrderID,
		arg.CustomerID,
		arg.Reason,
	)
	var i OrderReturn
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.OrderID,
		&i.CustomerID,
		&i.Reason,
		&i.Status,
		&i.PaymobRefundID,
		&i.RefundAmount,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getReturn = `-- name: GetReturn :one
SELECT id, shop_id, order_id, customer_id, reason, status, paymob_refund_id, refund_amount, notes, created_at, updated_at FROM order_returns
WHERE id = $1 AND shop_id = $2
`

type GetReturnParams struct {
	ID     uuid.UUID `json:"id"`
	ShopID uuid.UUID `json:"shop_id"`
}

func (q *Queries) GetReturn(ctx context.Context, arg GetReturnParams) (OrderReturn, error) {
	row := q.db.QueryRow(ctx, getReturn, arg.ID, arg.ShopID)
	var i OrderReturn
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.OrderID,
		&i.CustomerID,
		&i.Reason,
		&i.Status,
		&i.PaymobRefundID,
		&i.RefundAmount,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listReturns = `-- name: ListReturns :many
SELECT id, shop_id, order_id, customer_id, reason, status, paymob_refund_id, refund_amount, notes, created_at, updated_at FROM order_returns
WHERE shop_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type ListReturnsParams struct {
	ShopID uuid.UUID `json:"shop_id"`
	Limit  int32     `json:"limit"`
	Offset int32     `json:"offset"`
}

func (q *Queries) ListReturns(ctx context.Context, arg ListReturnsParams) ([]OrderReturn, error) {
	rows, err := q.db.Query(ctx, listReturns, arg.ShopID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OrderReturn
	for rows.Next() {
		var i OrderReturn
		if err := rows.Scan(
			&i.ID,
			&i.ShopID,
			&i.OrderID,
			&i.CustomerID,
			&i.Reason,
			&i.Status,
			&i.PaymobRefundID,
			&i.RefundAmount,
			&i.Notes,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listReturnsByOrder = `-- name: ListReturnsByOrder :many
SELECT id, shop_id, order_id, customer_id, reason, status, paymob_refund_id, refund_amount, notes, created_at, updated_at FROM order_returns
WHERE order_id = $1 AND shop_id = $2
ORDER BY created_at DESC
`

type ListReturnsByOrderParams struct {
	OrderID uuid.UUID `json:"order_id"`
	ShopID  uuid.UUID `json:"shop_id"`
}

func (q *Queries) ListReturnsByOrder(ctx context.Context, arg ListReturnsByOrderParams) ([]OrderReturn, error) {
	rows, err := q.db.Query(ctx, listReturnsByOrder, arg.OrderID, arg.ShopID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OrderReturn
	for rows.Next() {
		var i OrderReturn
		if err := rows.Scan(
			&i.ID,
			&i.ShopID,
			&i.OrderID,
			&i.CustomerID,
			&i.Reason,
			&i.Status,
			&i.PaymobRefundID,
			&i.RefundAmount,
			&i.Notes,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setReturnRefunded = `-- name: SetReturnRefunded :one
UPDATE order_returns
SET status           = 'refunded',
    paymob_refund_id = $3,
    refund_amount    = $4,
    updated_at       = NOW()
WHERE id = $1 AND shop_id = $2
RETURNING id, shop_id, order_id, customer_id, reason, status, paymob_refund_id, refund_amount, notes, created_at, updated_at
`

type SetReturnRefundedParams struct {
	ID             uuid.UUID      `json:"id"`
	ShopID         uuid.UUID      `json:"shop_id"`
	PaymobRefundID pgtype.Int8    `json:"paymob_refund_id"`
	RefundAmount   pgtype.Numeric `json:"refund_amount"`
}

func (q *Queries) SetReturnRefunded(ctx context.Context, arg SetReturnRefundedParams) (OrderReturn, error) {
	row := q.db.QueryRow(ctx, setReturnRefunded,
		arg.ID,
		arg.ShopID,
		arg.PaymobRefundID,
		arg.RefundAmount,
	)
	var i OrderReturn
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.OrderID,
		&i.CustomerID,
		&i.Reason,
		&i.Status,
		&i.PaymobRefundID,
		&i.RefundAmount,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateReturnStatus = `-- name: UpdateReturnStatus :one
UPDATE order_returns
SET status     = $3,
    notes      = COALESCE($4, notes),
    updated_at = NOW()
WHERE id = $1 AND shop_id = $2
RETURNING id, shop_id, order_id, customer_id, reason, status, paymob_refund_id, refund_amount, notes, created_at, updated_at
`

type UpdateReturnStatusParams struct {
	ID     uuid.UUID    `json:"id"`
	ShopID uuid.UUID    `json:"shop_id"`
	Status ReturnStatus `json:"status"`
	Notes  pgtype.Text  `json:"notes"`
}

func (q *Queries) UpdateReturnStatus(ctx context.Context, arg UpdateReturnStatusParams) (OrderReturn, error) {
	row := q.db.QueryRow(ctx, updateReturnStatus,
		arg.ID,
		arg.ShopID,
		arg.Status,
		arg.Notes,
	)
	var i OrderReturn
	err := row.Scan(
		&i.ID,
		&i.ShopID,
		&i.OrderID,
		&i.CustomerID,
		&i.Reason,
		&i.Status,
		&i.PaymobRefundID,
		&i.RefundAmount,
		&i.Notes,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
